<h3 class="scroll-m-20 pb-2 text-4xl font-semibold tracking-tight transition-colors first:mt-0">
	Introduction
</h3>
<p class="text-muted-foreground mb-8 border-b text-xl">Get started quickly and efficiently</p>

<p>Step 1: Install via npm</p>

<p class="mb-4">Open your terminal and run the following command:</p>
<pre><code>npm install svelte-table</code></pre>

<p class="mb-4">Step 2: Import it into your project</p>
<p class="mb-4">
	Once you’ve got the library installed, you can start using it in your Svelte components. Just
	import it like this:
</p>
<pre>{@html `<code>import {createTable} from 'svelte-table';</code>`}</pre>

<p class="mb-4">Step 3: Start building!</p>

<p class="mb-4">
	Now you’re all set! Use the <code>&lt;Table&gt;</code> component in your Svelte files to create your
	awesome data tables. Dive into the examples in our documentation to see all the cool things you can
	do!
</p>
